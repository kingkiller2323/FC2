{
  "name": "earthb-spike-visibility",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Earth-B visibility model spike — narrative reach + reception resolution. Pairs with spike-exposure; neither promotes alone.",
  "scripts": { "test": "vitest run" },
  "devDependencies": { "typescript": "^5.6.0", "tsx": "^4.19.0", "vitest": "^2.1.0", "fast-check": "^3.22.0" }
}
